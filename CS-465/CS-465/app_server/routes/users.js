//place holder until we get user logic
const express = require("express");
const router = express.Router();

// Placeholder for user-related routes
router.get("/", (req, res) => {
  res.send("User route placeholder");
});

module.exports = router;
